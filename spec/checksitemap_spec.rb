require 'spec_helper'

describe CheckSitemap do
  it 'has a version number' do
    expect(CheckSitemap::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
